import { task } from 'hardhat/config';


task("deploy-defi-adapters", "Inserts ")
    .addParam("registry", "The registry contract address")
    .addParam("harvestCodeProvider", "The harvest code provider contract")
    .setAction(async (taskArgs, localBRE) => {
        
    })