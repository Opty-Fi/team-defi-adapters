{
  "title": "Protection of mintOpty function (OPTYDistributor)",
  "description": "Test cases to check that non-staking pools addresses can't mint OPTY tokens",
  "stories": [
    {
      "description": "Non-staking vault addresses shouldn't be able to mint OPTY tokens from OPTYDistributor",
      "setActions": [
        {
          "contract": "optyDistributor",
          "action": "mintOpty(address,uint256)",
          "args": {
            "contractName": "priceOracle",
            "amount": "1000000000000000000"
          },
          "executor": "owner",
          "expect": "fail",
          "message": "caller is not a staking vault"
        }
      ],
      "getActions": []
    }
  ]
}
