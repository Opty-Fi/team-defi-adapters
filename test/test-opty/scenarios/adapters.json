{
  "title": "Adapters",
  "description": "Test cases for all adapters.",
  "stories": [
    {
      "description": "User should get correct code in getDepositSomeCodes()",
      "actions": [
        {
          "contract": "adapter",
          "action": "getDepositSomeCodes(address,address[],address,uint256[])",
          "args": {
            "amount": {
              "DAI": "2000000000000000000",
              "USDC": "2000000",
              "USDT": "2000000",
              "SLP_WETH_USDC": "2000000000000"
            }
          }
        }
      ]
    },
    {
      "description": "User should get correct code in getDepositAllCodes()",
      "actions": [
        {
          "contract": "adapter",
          "action": "getDepositAllCodes(address,address[],address)",
          "args": {}
        }
      ]
    },
    {
      "description": "User should get correct code in getWithdrawAllCodes()",
      "actions": [
        {
          "contract": "adapter",
          "action": "getWithdrawAllCodes(address,address[],address)",
          "args": {}
        }
      ]
    },
    {
      "description": "User should get correct code in getWithdrawSomeCodes()",
      "actions": [
        {
          "contract": "adapter",
          "action": "getWithdrawSomeCodes(address,address[],address,uint256)",
          "args": {
            "amount": {
              "DAI": "2000000000000000000",
              "USDC": "2000000",
              "USDT": "2000000",
              "SLP_WETH_USDC": "2000000000000"
            }
          }
        }
      ]
    },
    {
      "description": "User should get correct code in getBorrowAllCodes()",
      "actions": [
        {
          "contract": "adapter",
          "action": "getBorrowAllCodes(address,address[],address,address)",
          "args": {}
        }
      ]
    },
    {
      "description": "User should get correct code in getRepayAndWithdrawAllCodes()",
      "actions": [
        {
          "contract": "adapter",
          "action": "getRepayAndWithdrawAllCodes(address,address[],address,address)",
          "args": {}
        }
      ]
    }
  ]
}
